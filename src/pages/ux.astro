---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

const posts = (await getCollection('ux')).sort(
    (a: any, b: any) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout>
	<main>
		<section>
			<ul>
				{
					posts.map((post: any) => (
						<li>
							<a href={`/ux/${post.id}/`}>
								{post.data.heroImage && (
									<Image width={1440} height={720} src={post.data.heroImage} alt="" />
								)}
								<h4 class="title">{post.data.title}</h4>
								{/* <p class="date">
									<FormattedDate date={post.data.pubDate} />
								</p> */}
							</a>
						</li>
					))
				}
			</ul>
		</section>
	</main>
</Layout>

		<style>
			main {
				width: 960px;
			}
			ul {
				display: flex;
				flex-wrap: wrap;
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				width: calc(50% - 1rem);
				transition: transform 0.3s ease-in-out;
			}

			ul li * {
				text-decoration: none;
				transition: 0.2s ease;
			}
			ul li:first-child {
				width: 100%;
				margin-bottom: 1rem;
				text-align: center;
			}
			ul li:first-child img {
				width: 100%;
				height: 25em;
				margin-bottom: 1rem;
				object-fit: cover;
			}
			ul li:first-child .title {
				font-size: 1.5rem;
			}
			ul li img {
				margin-bottom: 0.8rem;
				border-radius: 12px;
				width: 100%;
				height: 15em;
				object-fit: cover;
				object-position: top;
				box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);

			}
			ul li a {
				display: block;
			}
			.title {
				margin: 0;
				color: rgb(var(--black));
				line-height: 1;
			}
			.date {
				margin: 0;
				color: rgb(var(--gray));
			}

			ul li:hover {
				transform: scale(1.02);
			}

			ul li a:hover h4,
			ul li a:hover .date {
				color: rgb(var(--accent));
			}
			ul a:hover img {
				box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);

			}
			@media (max-width: 720px) {
				ul {
					gap: 0.5em;
				}
				ul li {
					width: 100%;
					text-align: center;
				}
				ul li:first-child {
					margin-bottom: 0;
				}
				ul li:first-child .title {
					font-size: 1em;
				}
			}
		</style>

<!-- <style>
	main {
		width: 960px;
	}
	ul {
		display: grid
		/* display: flex;
		flex-direction: column; */
		/* flex-wrap: wrap; */
		gap: 2rem;
		list-style-type: none;
		margin: 0;
		padding: 0;
	}
	ul li {
		width: calc(50% - 1rem);
	}
	ul li * {
		text-decoration: none;
		transition: 0.2s ease;
	}
	ul li img {
		margin-bottom: 0.5rem;
		border-radius: 12px;
	}
	ul li a {
		display: block;
	}
	.title {
		margin: 0;
		line-height: 1;
	}

	ul li a:hover h4,
	ul li a:hover .date {
		color: rgb(var(--accent));
	}
	ul a:hover img {
		box-shadow: var(--box-shadow);
	}
	@media (max-width: 720px) {
		ul {
			gap: 0.5em;
		}
		ul li {
			width: 100%;
			text-align: center;
		}
		ul li:first-child {
			margin-bottom: 0;
		}
		ul li:first-child .title {
			font-size: 1.563em;
		}
	}
</style> -->